<h2>ジャンル編集</h2>

  <%= form_with model: @genre, url: update_genere_path(@genre.id), method: :patch do |f| %>
    <h4>ジャンル名</h4>
      <%= f.text_area :name %>
      <%= f.submit '変更を保存' %>
  <% end %>